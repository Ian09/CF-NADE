"""Output of Data.py"""

(input_matrix, output_vector)

where:
  input_matrix: Batch_size * movie_dim * 2, one contains the ratings and one contains the timestamp difference.
  output_vector: Batch_size, a the output rating for every input

